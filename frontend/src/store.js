import { createStore } from 'vuex';

const store = createStore({
  state() {
    return {
      // Votre état initial
    };
  },
  mutations: {
    // Vos mutations
  },
  actions: {
    // Vos actions
  },
  getters: {
    // Vos getters
  }
});

export default store;
